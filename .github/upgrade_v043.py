from pathlib import Path


def replace_once(text, old, new, label):
    if new in text and old not in text:
        return text
    if old not in text:
        raise SystemExit(f'v0.43 patch marker missing: {label}')
    return text.replace(old, new, 1)

p = Path('index.html')
text = p.read_text()

if '/* v0.43 — Circuit Builder */' not in text:
    text = text.replace('v0.42', 'v0.43')

    css = r'''
/* v0.43 — Circuit Builder */
#circuitBuilder{position:fixed;inset:0;z-index:80;background:#e9eef4;color:#182332;display:flex;flex-direction:column;overflow:hidden}
#circuitBuilder[hidden]{display:none}.cbTop{display:flex;align-items:center;gap:12px;background:#16283e;color:white;padding:calc(10px + env(safe-area-inset-top)) 12px 10px}.cbTop strong{font-size:15px;letter-spacing:.06em}.cbTop small{display:block;color:#b8c8d9;font-size:10px;margin-top:2px}.cbTop .cbGrow{flex:1}.cbTop button{background:#29425e;color:white}.cbShell{display:grid;grid-template-columns:260px minmax(0,1fr);min-height:0;flex:1}.cbSide{background:#fff;border-right:1px solid #ccd7e1;padding:12px;overflow:auto}.cbSide h3{font-size:11px;text-transform:uppercase;letter-spacing:.08em;color:#65768a;margin:6px 0 8px}.cbStage{position:relative;min-width:0;min-height:0;padding:12px;display:flex;flex-direction:column;gap:10px}.cbCard{border:1px solid #d4dee8;background:#fff;border-radius:14px;padding:12px;margin-bottom:8px}.cbCard strong{display:block}.cbCard small{display:block;color:#65768a;margin-top:3px;line-height:1.35}.cbCard button{margin-top:8px;background:#eaf0f6}.cbCard button.primary{background:#1c5e93;color:#fff}.cbCircuitRow{display:grid;grid-template-columns:12px minmax(0,1fr) auto;gap:8px;align-items:center;border:1px solid #d7e1ea;background:#fff;border-radius:11px;padding:9px;margin-bottom:7px}.cbDot{width:10px;height:10px;border-radius:50%;background:var(--cb-color,#65768a)}.cbCircuitRow button{padding:7px 9px;background:#edf2f7;font-size:11px}.cbCircuitRow.done{box-shadow:inset 0 0 0 1px #39a467}.cbLanding{overflow:auto;max-width:980px;width:100%;margin:0 auto}.cbLanding h1{margin:4px 0 5px;font-size:clamp(22px,4vw,32px)}.cbLanding>p{color:#5b6e82;line-height:1.45;margin-top:0}.cbModeGrid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.cbModePanel{background:#fff;border:1px solid #d3dee8;border-radius:16px;padding:15px}.cbModePanel h2{font-size:17px;margin:0 0 5px}.cbModePanel>p{font-size:12px;color:#607185;line-height:1.45}.cbZoneButtons{display:grid;gap:7px}.cbZoneButton{display:flex;align-items:center;justify-content:space-between;gap:10px;text-align:left;background:#edf2f7}.cbZoneButton.done{background:#e9f7ee;color:#195d35}.cbGame{display:flex;min-height:0;flex:1;flex-direction:column;gap:8px}.cbGameHeader{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.cbGameHeader strong{font-size:16px}.cbGameHeader .cbGrow{flex:1}.cbPill{font-size:11px;background:#fff;border:1px solid #cedae5;border-radius:999px;padding:6px 9px}.cbBoardWrap{position:relative;min-height:260px;flex:1;border-radius:18px;border:1px solid #c8d5e0;background:#fff;overflow:hidden;touch-action:none;box-shadow:0 6px 24px #16283e0b}.cbBoardWrap canvas{width:100%;height:100%;display:block;touch-action:none}.cbBoardHint{position:absolute;left:50%;top:10px;transform:translateX(-50%);background:#16283ee9;color:#fff;border-radius:999px;padding:7px 11px;font-size:11px;pointer-events:none;max-width:90%;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.cbActions{display:flex;gap:7px;align-items:center;flex-wrap:wrap}.cbActions button{background:#e8eef5}.cbActions .primary{background:#1c5e93;color:white}.cbActions .complete{background:#25834e;color:white}.cbProgress{font-size:12px;color:#5f7184;margin-left:auto}.cbCelebrate{position:absolute;inset:0;z-index:3;display:flex;align-items:center;justify-content:center;background:#16283e28;pointer-events:none}.cbCelebrate div{background:#fff;border-radius:18px;padding:20px 28px;box-shadow:0 15px 50px #16283e3f;font-size:19px;font-weight:850;animation:cbPop .35s ease-out}@keyframes cbPop{from{transform:scale(.72);opacity:.2}to{transform:scale(1);opacity:1}}.cbFinish{width:100%;margin-top:10px;background:#25834e!important;color:#fff!important;min-height:46px}.cbFinish:disabled{background:#dbe3ea!important;color:#8895a2!important}.cbAsFit{display:flex;min-height:0;flex:1;flex-direction:column;gap:8px}.cbAsFit canvas{width:100%;height:100%;display:block}.cbAsFitWrap{min-height:280px;flex:1;background:#fff;border:1px solid #c8d5e0;border-radius:16px;overflow:hidden}.cbEmpty{font-size:12px;color:#718195;padding:10px 3px}.cbSelectCount{font-weight:800}.cbStage [hidden]{display:none!important}
@media(max-width:760px){.cbShell{grid-template-columns:1fr}.cbSide{display:none}.cbStage{padding:7px}.cbModeGrid{grid-template-columns:1fr}.cbLanding{padding-bottom:18px}.cbBoardWrap{min-height:330px}.cbActions button{min-height:42px}.cbGameHeader{font-size:12px}.cbGameHeader strong{font-size:14px}.cbProgress{width:100%;margin-left:0}.cbTop{padding-left:8px;padding-right:8px}}
'''
    text = text.replace('</style>', css + '\n</style>', 1)

    text = replace_once(
        text,
        'The same plan can be used for the zone plan and the site survey.',
        'The same plan feeds the zone plan, site survey and Circuit Builder.',
        'home intro'
    )
    text = replace_once(
        text,
        '<button id="surveyModeBtn" aria-pressed="false">Survey mode</button><button id="projectMenuBtn"',
        '<button id="surveyModeBtn" aria-pressed="false">Survey mode</button><button id="circuitModeBtn">Circuit Builder</button><button id="projectMenuBtn"',
        'Circuit Builder top button'
    )

    overlay = r'''
<section id="circuitBuilder" hidden aria-label="Circuit Builder">
<header class="cbTop"><div><strong>CIRCUIT BUILDER</strong><small>ZONE PLAN + SURVEY → WIRING → AS-FIT</small></div><div class="cbGrow"></div><button id="cbBack">Back to drawing</button></header>
<div class="cbShell">
<aside class="cbSide"><h3>Circuits on this floor</h3><div id="cbExisting"></div><button id="cbFinishAsFit" class="cbFinish" disabled>Finish As-Fit</button><p id="cbFinishStatus" class="cbEmpty">Complete the circuits you create, then build the As-Fit.</p></aside>
<main class="cbStage">
<section id="cbLanding" class="cbLanding"><h1>Turn the survey into the circuit</h1><p>The room layout stays recognisable behind the devices, but the devices spread out just enough to make tracing the wiring clear and satisfying.</p><div class="cbModeGrid"><div class="cbModePanel"><h2>Conventional</h2><p>Choose a zone. Circuit Builder automatically pulls in the surveyed devices that sit inside that zone.</p><div id="cbConventionalZones" class="cbZoneButtons"></div></div><div class="cbModePanel"><h2>Addressable</h2><p>Highlight the devices that belong to the loop by tapping or sweeping across them on the plan, then build the loop.</p><button id="cbAddressableStart" class="primary">Highlight a loop</button></div></div></section>
<section id="cbGame" class="cbGame" hidden><div class="cbGameHeader"><button id="cbGameHome">← Circuits</button><strong id="cbCircuitTitle">Circuit</strong><span id="cbCircuitType" class="cbPill"></span><span class="cbGrow"></span><span id="cbSelectCount" class="cbPill cbSelectCount" hidden></span></div><div id="cbBoardWrap" class="cbBoardWrap"><canvas id="cbCanvas"></canvas><div id="cbBoardHint" class="cbBoardHint"></div><div id="cbCelebrate" class="cbCelebrate" hidden><div id="cbCelebrateText">Circuit complete ✓</div></div></div><div class="cbActions"><button id="cbBuildSelected" class="primary" hidden>Build highlighted loop</button><button id="cbUndoLeg">Undo leg</button><button id="cbReset">Reset</button><button id="cbComplete" class="complete">Complete circuit</button><span id="cbProgress" class="cbProgress"></span></div></section>
<section id="cbAsFit" class="cbAsFit" hidden><div class="cbGameHeader"><button id="cbAsFitBack">← Circuits</button><strong>As-Fit preview</strong><span class="cbGrow"></span><button id="cbExportAsFit" class="primary">Export As-Fit PNG</button></div><div class="cbAsFitWrap"><canvas id="cbAsFitCanvas"></canvas></div></section>
</main></div></section>
'''
    text = replace_once(text, '</div></section>\n<div class="app">', '</div></section>\n' + overlay + '<div class="app">', 'Circuit Builder overlay')

    text = replace_once(
        text,
        "symbols:[],pins:[],pictureOpacity:1",
        "symbols:[],pins:[],circuits:[],asFit:null,pictureOpacity:1",
        'fresh circuit state'
    )
    text = replace_once(
        text,
        "'symbols','pins','pictureOpacity'",
        "'symbols','pins','circuits','asFit','pictureOpacity'",
        'floor keys'
    )
    text = replace_once(
        text,
        "pins:f?.data?.pins||[]}}",
        "pins:f?.data?.pins||[],circuits:f?.data?.circuits||[],asFit:f?.data?.asFit||null}}",
        'floor view circuit data'
    )

    js = r'''
// v0.43 Circuit Builder. Kept in a separate full-screen surface so Zone Plan and Survey behaviour stay untouched.
let cbScreen='home',cbCircuit=null,cbDrag=null,cbHover=null,cbSelection=new Set(),cbSelectBounds=null;
const CB_ROUTE_GRID=20,CB_INSET=.08,CB_SPAN=.84,CB_COLORS=['#e33a3a','#2675db','#20a36b','#dc9c16','#9957c5','#e26929','#1d9baa','#d04a88'];
function cbCircuits(){if(!Array.isArray(state.circuits))state.circuits=[];return state.circuits}
function cbPanels(){return(state.symbols||[]).filter(s=>s.type==='panel')}
function cbSurveyDevices(){return(state.symbols||[]).filter(s=>symbolScope(s)==='survey'&&s.type!=='panel'&&s.type!=='you')}
function cbSymbol(id){return(state.symbols||[]).find(s=>s.id===id)||null}
function cbZoneShapes(zoneId){return(state.shapes||[]).filter(s=>s.zone===zoneId&&s.points?.length>=3)}
function cbZoneDevices(zoneId){const shapes=cbZoneShapes(zoneId);return cbSurveyDevices().filter(d=>shapes.some(sh=>pointInPoly(d,sh.points)))}
function cbBounds(points,margin=.07){const pts=(points||[]).filter(p=>Number.isFinite(p?.x)&&Number.isFinite(p?.y));if(!pts.length)return{x1:0,y1:0,x2:1,y2:1};let x1=Math.min(...pts.map(p=>p.x)),x2=Math.max(...pts.map(p=>p.x)),y1=Math.min(...pts.map(p=>p.y)),y2=Math.max(...pts.map(p=>p.y));let cx=(x1+x2)/2,cy=(y1+y2)/2,w=Math.max(.22,x2-x1),h=Math.max(.22,y2-y1);x1=cx-w/2-margin;x2=cx+w/2+margin;y1=cy-h/2-margin;y2=cy+h/2+margin;if(x1<0){x2-=x1;x1=0}if(y1<0){y2-=y1;y1=0}if(x2>1){x1-=x2-1;x2=1}if(y2>1){y1-=y2-1;y2=1}return{x1:clamp(x1),y1:clamp(y1),x2:clamp(x2),y2:clamp(y2)}}
function cbBoundsFor(zoneId,devices,panel){const zpts=zoneId?cbZoneShapes(zoneId).flatMap(s=>s.points):[];return cbBounds(zpts.length?zpts:[...devices,panel].filter(Boolean),zoneId?.05:.08)}
function cbPlanToBoard(p,b){const w=Math.max(.001,b.x2-b.x1),h=Math.max(.001,b.y2-b.y1);return{x:clamp(CB_INSET+(p.x-b.x1)/w*CB_SPAN,.04,.96),y:clamp(CB_INSET+(p.y-b.y1)/h*CB_SPAN,.04,.96)}}
function cbBoardToPlan(p,b){const w=Math.max(.001,b.x2-b.x1),h=Math.max(.001,b.y2-b.y1);return{x:clamp(b.x1+(p.x-CB_INSET)/CB_SPAN*w),y:clamp(b.y1+(p.y-CB_INSET)/CB_SPAN*h)}}
function cbNearestPanel(devices){const panels=cbPanels();if(!panels.length)return null;const pts=devices?.length?devices:panels,cx=pts.reduce((n,p)=>n+p.x,0)/pts.length,cy=pts.reduce((n,p)=>n+p.y,0)/pts.length;return panels.slice().sort((a,b)=>Math.hypot(a.x-cx,a.y-cy)-Math.hypot(b.x-cx,b.y-cy))[0]}
function cbMakeLayout(ids,bounds){const layout={},anchors={};for(const id of ids){const s=cbSymbol(id);if(!s)continue;layout[id]=anchors[id]=cbPlanToBoard(s,bounds)}const keys=Object.keys(layout),gap=Math.max(.05,Math.min(.09,.36/Math.sqrt(Math.max(1,keys.length))));for(let pass=0;pass<24;pass++){for(let i=0;i<keys.length;i++)for(let j=i+1;j<keys.length;j++){const a=layout[keys[i]],b=layout[keys[j]],dx=b.x-a.x,dy=b.y-a.y,d=Math.hypot(dx,dy);if(d<gap){const ang=d<.0001?(i*1.7+j*.9):Math.atan2(dy,dx),push=(gap-Math.max(d,.001))*.42,px=Math.cos(ang)*push,py=Math.sin(ang)*push;a.x-=px;a.y-=py;b.x+=px;b.y+=py}}for(const id of keys){const p=layout[id],a=anchors[id];p.x+=((a.x-p.x)*.12);p.y+=((a.y-p.y)*.12);p.x=clamp(p.x,Math.max(.045,a.x-.075),Math.min(.955,a.x+.075));p.y=clamp(p.y,Math.max(.045,a.y-.075),Math.min(.955,a.y+.075))}}return layout}
function cbNewCircuit(type,devices,zoneId=null){const panel=cbNearestPanel(devices);if(!panel){alert('Add a Panel symbol to the plan or survey first. Circuit Builder needs a start point.');return null}const bounds=cbBoundsFor(zoneId,devices,panel),index=cbCircuits().filter(c=>c.type===type).length+1,z=zoneId?(state.zones||[]).find(z=>z.id===zoneId):null,c={id:uid(),type,zoneId,panelId:panel.id,deviceIds:devices.map(d=>d.id),bounds,layout:null,sequence:[panel.id],legs:[],complete:false,eolId:null,color:z?.color||CB_COLORS[(index-1)%CB_COLORS.length],name:type==='conventional'?'Zone '+(z?.number||index):'Loop '+index,updatedAt:Date.now()};c.layout=cbMakeLayout([panel.id,...c.deviceIds],bounds);push();cbCircuits().push(c);changed();return c}
function cbExistingConventional(zoneId){return cbCircuits().find(c=>c.type==='conventional'&&c.zoneId===zoneId)}
function cbUsedIds(){return new Set(cbCircuits().flatMap(c=>c.deviceIds||[]))}
function cbRenderLanding(){const zones=$('cbConventionalZones'),existing=$('cbExisting'),list=cbCircuits();zones.textContent='';for(const z of state.zones||[]){const devices=cbZoneDevices(z.id),c=cbExistingConventional(z.id);const b=document.createElement('button');b.className='cbZoneButton'+(c?.complete?' done':'');const left=document.createElement('span');left.textContent='Zone '+z.number+(z.name?' · '+z.name:'');const right=document.createElement('span');right.textContent=c?.complete?'✓ '+devices.length:devices.length+' device'+(devices.length===1?'':'s');b.append(left,right);b.disabled=!devices.length;b.onclick=()=>{if(c)cbOpenCircuit(c);else{const made=cbNewCircuit('conventional',devices,z.id);if(made)cbOpenCircuit(made)}};zones.append(b)}if(!(state.zones||[]).length){const e=document.createElement('div');e.className='cbEmpty';e.textContent='Add zone areas in Plan / Zone Maker first.';zones.append(e)}existing.textContent='';for(const c of list){const row=document.createElement('div');row.className='cbCircuitRow'+(c.complete?' done':'');row.style.setProperty('--cb-color',c.color||'#65768a');const dot=document.createElement('span');dot.className='cbDot';const label=document.createElement('div');const s=document.createElement('strong');s.textContent=c.name;const small=document.createElement('small');small.textContent=(c.type==='addressable'?'Addressable loop':'Conventional')+' · '+(c.deviceIds?.length||0)+' devices'+(c.complete?' · complete':'');label.append(s,small);const open=document.createElement('button');open.textContent='Open';open.onclick=()=>cbOpenCircuit(c);row.append(dot,label,open);existing.append(row)}if(!list.length){const e=document.createElement('div');e.className='cbEmpty';e.textContent='No circuits built yet.';existing.append(e)}const ready=list.length>0&&list.every(c=>c.complete);$('cbFinishAsFit').disabled=!ready;$('cbFinishStatus').textContent=ready?'All created circuits are complete. Finish builds the As-Fit from the real surveyed positions.':list.length?'Complete every circuit above before finishing the As-Fit.':'Complete the circuits you create, then build the As-Fit.'}
function cbShow(screen){cbScreen=screen;$('cbLanding').hidden=screen!=='home';$('cbGame').hidden=!(screen==='game'||screen==='select');$('cbAsFit').hidden=screen!=='asfit';if(screen==='home')cbRenderLanding();if(screen==='asfit')requestAnimationFrame(cbDrawAsFitPreview)}
function openCircuitBuilder(){if(!img){alert('Start or import a floor plan first.');return}syncFloor();cbCircuits();closeToolMenus();$('circuitBuilder').hidden=false;document.querySelector('.app').inert=true;cbCircuit=null;cbSelection.clear();cbShow('home')}
function closeCircuitBuilder(){$('circuitBuilder').hidden=true;document.querySelector('.app').inert=false;cbDrag=null;cbHover=null;saveNow();draw()}
function cbOpenCircuit(c){cbCircuit=c;cbDrag=null;cbHover=null;cbSelection.clear();$('cbCircuitTitle').textContent=c.name;$('cbCircuitType').textContent=c.type==='addressable'?'ADDRESSABLE LOOP':'CONVENTIONAL';$('cbSelectCount').hidden=true;$('cbBuildSelected').hidden=true;$('cbUndoLeg').hidden=false;$('cbReset').hidden=false;$('cbComplete').hidden=false;cbShow('game');cbUpdateGame();requestAnimationFrame(cbDrawBoard)}
function cbStartAddressable(){const devices=cbSurveyDevices();if(!devices.length){alert('Place the surveyed devices first.');return}if(!cbPanels().length){alert('Add a Panel symbol first.');return}cbCircuit=null;cbSelection.clear();cbSelectBounds=cbBounds([...devices,...cbPanels()],.06);$('cbCircuitTitle').textContent='Highlight addressable loop';$('cbCircuitType').textContent='SWIPE / TAP DEVICES';$('cbSelectCount').hidden=false;$('cbBuildSelected').hidden=false;$('cbUndoLeg').hidden=true;$('cbReset').hidden=false;$('cbComplete').hidden=true;cbShow('select');cbUpdateGame();requestAnimationFrame(cbDrawBoard)}
function cbBuildAddressable(){const ids=[...cbSelection],devices=ids.map(cbSymbol).filter(Boolean);if(!devices.length)return;const c=cbNewCircuit('addressable',devices);if(c)cbOpenCircuit(c)}
function cbCanvasSize(c){const r=c.getBoundingClientRect(),dpr=Math.min(devicePixelRatio||1,2),w=Math.max(1,r.width),h=Math.max(1,r.height);if(c.width!==Math.round(w*dpr))c.width=Math.round(w*dpr);if(c.height!==Math.round(h*dpr))c.height=Math.round(h*dpr);const x=c.getContext('2d');x.setTransform(dpr,0,0,dpr,0,0);x.clearRect(0,0,w,h);return{x,w,h}}
function cbBoardPx(p,w,h){const pad=28;return{x:pad+p.x*(w-pad*2),y:pad+p.y*(h-pad*2)}}
function cbPxBoard(p,w,h){const pad=28;return{x:clamp((p.x-pad)/Math.max(1,w-pad*2)),y:clamp((p.y-pad)/Math.max(1,h-pad*2))}}
function cbPlanPx(p,b,w,h){return cbBoardPx(cbPlanToBoard(p,b),w,h)}
function cbGhost(x,w,h,bounds,zoneId){x.save();x.fillStyle='#fff';x.fillRect(0,0,w,h);x.beginPath();x.rect(18,18,w-36,h-36);x.clip();if(zoneId){for(const sh of cbZoneShapes(zoneId)){const pts=sh.points.map(p=>cbPlanPx(p,bounds,w,h)),z=(state.zones||[]).find(z=>z.id===zoneId);x.beginPath();pts.forEach((p,i)=>i?x.lineTo(p.x,p.y):x.moveTo(p.x,p.y));x.closePath();x.globalAlpha=.09;x.fillStyle=z?.color||'#2675db';x.fill();x.globalAlpha=.35;x.strokeStyle=z?.color||'#2675db';x.lineWidth=2;x.stroke()}}x.globalAlpha=.42;x.strokeStyle='#566a7f';x.lineWidth=1.6;for(const wall of state.walls||[]){if(wall.kind!=='wall'||wall.points?.length<2)continue;const pts=wall.points.map(p=>cbPlanPx(p,bounds,w,h));x.beginPath();pts.forEach((p,i)=>i?x.lineTo(p.x,p.y):x.moveTo(p.x,p.y));x.stroke()}x.globalAlpha=.55;x.fillStyle='#53667b';x.font='600 11px system-ui';x.textAlign='center';for(const l of state.labels||[]){if(l.x<bounds.x1||l.x>bounds.x2||l.y<bounds.y1||l.y>bounds.y2)continue;const p=cbPlanPx(l,bounds,w,h);x.fillText(l.text,p.x,p.y)}x.restore()}
function cbNodeBoard(id){if(cbScreen==='select'){const s=cbSymbol(id);return s?cbPlanToBoard(s,cbSelectBounds):null}return cbCircuit?.layout?.[id]||null}
function cbNodePx(id,w,h){const p=cbNodeBoard(id);return p?cbBoardPx(p,w,h):null}
function cbDrawNode(x,s,p,r,ring,muted=false){x.save();if(ring){x.beginPath();x.arc(p.x,p.y,r+8,0,Math.PI*2);x.strokeStyle=ring;x.lineWidth=4;x.globalAlpha=.8;x.stroke()}x.globalAlpha=muted?.42:1;drawSymbol(x,p,s.type,r,s.color||'#172333',s.rotation||0);if(s.reference){x.font='700 10px system-ui';x.fillStyle='#40546a';x.textAlign='center';x.fillText(s.reference,p.x,p.y+r+15)}x.restore()}
function cbSegKey(a,b){const q=p=>Math.round(p.x*1000)+','+Math.round(p.y*1000),ka=q(a),kb=q(b);return ka<kb?ka+'|'+kb:kb+'|'+ka}
function cbDrawBundled(x,segments,map){const counts=new Map(),seen=new Map();for(const s of segments){const k=cbSegKey(s.a,s.b);counts.set(k,(counts.get(k)||0)+1)}for(const s of segments){const k=cbSegKey(s.a,s.b),n=counts.get(k)||1,i=seen.get(k)||0;seen.set(k,i+1);const a=map(s.a),b=map(s.b),dx=b.x-a.x,dy=b.y-a.y,L=Math.hypot(dx,dy)||1,off=(i-(n-1)/2)*7,nx=-dy/L,ny=dx/L;x.save();x.strokeStyle=s.color;x.lineWidth=5;x.lineCap='round';x.lineJoin='round';x.beginPath();x.moveTo(a.x+nx*off,a.y+ny*off);x.lineTo(b.x+nx*off,b.y+ny*off);x.stroke();x.restore()}}
function cbLegSegments(c){const out=[];for(const leg of c?.legs||[]){const pts=leg.points||[];for(let i=1;i<pts.length;i++)out.push({a:pts[i-1],b:pts[i],color:c.color||'#2675db'})}return out}
function cbValidTarget(id){const c=cbCircuit;if(!c||!id||id===c.sequence.at(-1))return false;const visited=new Set(c.sequence);if(id===c.panelId)return c.type==='addressable'&&c.deviceIds.every(x=>visited.has(x))&&c.sequence.length>1;return c.deviceIds.includes(id)&&!visited.has(id)}
function cbHitNode(pt,w,h,validOnly=false){const ids=cbScreen==='select'?[...cbSurveyDevices().map(s=>s.id),...cbPanels().map(s=>s.id)]:[cbCircuit?.panelId,...(cbCircuit?.deviceIds||[])].filter(Boolean);let best=null;for(const id of ids){if(validOnly&&!cbValidTarget(id))continue;const p=cbNodePx(id,w,h);if(!p)continue;const d=Math.hypot(pt.x-p.x,pt.y-p.y);if(d<=30&&(!best||d<best.d))best={id,d}}return best?.id||null}
function cbDrawBoard(){if($('cbGame').hidden)return;const c=$('cbCanvas'),{x,w,h}=cbCanvasSize(c),bounds=cbScreen==='select'?cbSelectBounds:cbCircuit?.bounds||{x1:0,y1:0,x2:1,y2:1};cbGhost(x,w,h,bounds,cbCircuit?.zoneId||null);if(cbScreen==='game'&&cbCircuit){cbDrawBundled(x,cbLegSegments(cbCircuit),p=>cbBoardPx(p,w,h));if(cbDrag?.points?.length>1){x.save();x.strokeStyle=cbCircuit.color||'#2675db';x.lineWidth=5;x.lineCap='round';x.lineJoin='round';x.globalAlpha=.65;x.beginPath();cbDrag.points.forEach((p,i)=>i?x.lineTo(p.x,p.y):x.moveTo(p.x,p.y));x.stroke();x.restore()}}const ids=cbScreen==='select'?[...cbSurveyDevices().map(s=>s.id),...cbPanels().map(s=>s.id)]:[cbCircuit?.panelId,...(cbCircuit?.deviceIds||[])].filter(Boolean),visited=new Set(cbCircuit?.sequence||[]),current=cbCircuit?.sequence?.at(-1);for(const id of ids){const s=cbSymbol(id),p=cbNodePx(id,w,h);if(!s||!p)continue;let ring=null;if(cbScreen==='select'&&cbSelection.has(id))ring='#2675db';else if(cbScreen==='game'&&id===current)ring='#20a36b';else if(cbScreen==='game'&&visited.has(id))ring='#8bbf9e';if(cbHover===id)ring='#dc9c16';cbDrawNode(x,s,p,s.type==='panel'?20:17,ring,cbScreen==='select'&&s.type==='panel')}if(cbScreen==='select')$('cbBoardHint').textContent='Tap or sweep across the devices in this loop';else if(cbCircuit?.complete)$('cbBoardHint').textContent='Circuit complete · reopen or finish the As-Fit';else $('cbBoardHint').textContent='Start at the green-ring device, drag a neat route and release on the next device'}
function cbGameCounts(){if(!cbCircuit)return{done:0,total:0,ready:false};const visited=new Set(cbCircuit.sequence),done=cbCircuit.deviceIds.filter(id=>visited.has(id)).length,total=cbCircuit.deviceIds.length,ready=cbCircuit.type==='conventional'?done===total:(done===total&&cbCircuit.sequence.at(-1)===cbCircuit.panelId&&cbCircuit.sequence.length>1);return{done,total,ready}}
function cbUpdateGame(){if(cbScreen==='select'){$('cbSelectCount').textContent=cbSelection.size+' highlighted';$('cbBuildSelected').disabled=cbSelection.size<1;$('cbProgress').textContent='Swipe across devices to select them';$('cbReset').textContent='Clear highlighted';requestAnimationFrame(cbDrawBoard);return}if(!cbCircuit)return;const q=cbGameCounts();$('cbProgress').textContent=q.done+' / '+q.total+' devices connected'+(cbCircuit.type==='addressable'&&q.done===q.total&&cbCircuit.sequence.at(-1)!==cbCircuit.panelId?' · return to panel':'');$('cbComplete').disabled=!q.ready||cbCircuit.complete;$('cbUndoLeg').disabled=!cbCircuit.legs.length;$('cbReset').textContent='Reset circuit';requestAnimationFrame(cbDrawBoard)}
function cbPointInCanvas(e){const r=$('cbCanvas').getBoundingClientRect();return{x:e.clientX-r.left,y:e.clientY-r.top}}
function cbSnapPx(p,w,h){const pad=28,x=clamp(Math.round((p.x-pad)/CB_ROUTE_GRID)*CB_ROUTE_GRID+pad,pad,w-pad),y=clamp(Math.round((p.y-pad)/CB_ROUTE_GRID)*CB_ROUTE_GRID+pad,pad,h-pad);return{x,y}}
function cbCollinear(a,b,c){return(Math.abs(a.x-b.x)<.5&&Math.abs(b.x-c.x)<.5)||(Math.abs(a.y-b.y)<.5&&Math.abs(b.y-c.y)<.5)}
function cbSimplify(points){const out=[];for(const p of points){const last=out.at(-1);if(last&&Math.hypot(last.x-p.x,last.y-p.y)<1)continue;out.push(p);while(out.length>=3&&cbCollinear(out.at(-3),out.at(-2),out.at(-1)))out.splice(out.length-2,1)}return out}
function cbAppendDrag(p,w,h){if(!cbDrag)return;const q=cbSnapPx(p,w,h),last=cbDrag.points.at(-1);if(!last||Math.hypot(q.x-last.x,q.y-last.y)<8)return;const dx=q.x-last.x,dy=q.y-last.y;if(Math.abs(dx)>1&&Math.abs(dy)>1){const prev=cbDrag.points.at(-2),horizontal=prev?Math.abs(last.y-prev.y)<Math.abs(last.x-prev.x):Math.abs(dx)>=Math.abs(dy),corner=horizontal?{x:q.x,y:last.y}:{x:last.x,y:q.y};cbDrag.points.push(corner)}cbDrag.points.push(q);cbDrag.points=cbSimplify(cbDrag.points)}
function cbFinishLeg(target,w,h){const c=cbCircuit,from=c.sequence.at(-1),end=cbNodePx(target,w,h),pts=cbDrag?.points?.slice()||[];if(!end||!pts.length)return;const last=pts.at(-1),dx=end.x-last.x,dy=end.y-last.y;if(Math.abs(dx)>1&&Math.abs(dy)>1){const prev=pts.at(-2),horizontal=prev?Math.abs(last.y-prev.y)<Math.abs(last.x-prev.x):Math.abs(dx)>=Math.abs(dy);pts.push(horizontal?{x:end.x,y:last.y}:{x:last.x,y:end.y})}pts.push(end);const normal=cbSimplify(pts).map(p=>cbPxBoard(p,w,h));c.legs.push({from,to:target,points:normal});c.sequence.push(target);c.complete=false;c.updatedAt=Date.now();persist();cbDrag=null;cbHover=null;cbUpdateGame()}
function cbSelectAt(pt,w,h){const id=cbHitNode(pt,w,h,false),s=cbSymbol(id);if(!id||!s||s.type==='panel')return false;cbSelection.add(id);cbUpdateGame();return true}
function cbCanvasDown(e){const c=$('cbCanvas'),r=c.getBoundingClientRect(),w=r.width,h=r.height,p=cbPointInCanvas(e);c.setPointerCapture?.(e.pointerId);if(cbScreen==='select'){cbDrag={select:true};cbSelectAt(p,w,h);return}if(cbScreen!=='game'||!cbCircuit||cbCircuit.complete)return;const start=cbCircuit.sequence.at(-1),sp=cbNodePx(start,w,h);if(!sp||Math.hypot(p.x-sp.x,p.y-sp.y)>34){$('cbBoardHint').textContent='Start your drag from the green-ring device';return}cbDrag={points:[sp],pointerId:e.pointerId};cbHover=null}
function cbCanvasMove(e){const r=$('cbCanvas').getBoundingClientRect(),w=r.width,h=r.height,p=cbPointInCanvas(e);if(cbScreen==='select'&&cbDrag?.select){cbSelectAt(p,w,h);return}if(cbScreen!=='game'||!cbDrag?.points)return;cbAppendDrag(p,w,h);cbHover=cbHitNode(p,w,h,true);cbDrawBoard()}
function cbCanvasUp(e){const r=$('cbCanvas').getBoundingClientRect(),w=r.width,h=r.height,p=cbPointInCanvas(e);if(cbScreen==='select'){cbDrag=null;return}if(cbScreen!=='game'||!cbDrag?.points)return;const target=cbHitNode(p,w,h,true);if(target)cbFinishLeg(target,w,h);else{cbDrag=null;cbHover=null;$('cbBoardHint').textContent='Release directly over the next device to lock the cable';cbDrawBoard()}}
function cbUndo(){if(!cbCircuit?.legs?.length)return;cbCircuit.legs.pop();cbCircuit.sequence.pop();cbCircuit.complete=false;cbCircuit.eolId=null;persist();cbUpdateGame()}
function cbResetCurrent(){if(cbScreen==='select'){cbSelection.clear();cbUpdateGame();return}if(!cbCircuit)return;cbCircuit.legs=[];cbCircuit.sequence=[cbCircuit.panelId];cbCircuit.complete=false;cbCircuit.eolId=null;persist();cbUpdateGame()}
function cbCelebrate(text){$('cbCelebrateText').textContent=text;$('cbCelebrate').hidden=false;try{navigator.vibrate?.(35)}catch(e){}setTimeout(()=>{$('cbCelebrate').hidden=true},900)}
function cbCompleteCircuit(){if(!cbCircuit)return;const q=cbGameCounts();if(!q.ready)return;push();cbCircuit.complete=true;cbCircuit.eolId=cbCircuit.type==='conventional'?cbCircuit.sequence.at(-1):null;cbCircuit.updatedAt=Date.now();changed();cbCelebrate(cbCircuit.type==='addressable'?'Loop complete ✓':'Circuit complete ✓');setTimeout(()=>{cbCircuit=null;cbShow('home')},850)}
function cbPlanPathForLeg(c,leg){const a=cbSymbol(leg.from),b=cbSymbol(leg.to);if(!a||!b)return[];let pts=(leg.points||[]).map(p=>cbBoardToPlan(p,c.bounds));if(pts.length<2)return[a,b];pts[0]={x:a.x,y:a.y};pts[pts.length-1]={x:b.x,y:b.y};const orth=[pts[0]];for(let i=1;i<pts.length;i++){const prev=orth.at(-1),q=pts[i];if(Math.abs(prev.x-q.x)>.0005&&Math.abs(prev.y-q.y)>.0005){const boardPrev=leg.points[Math.max(0,i-1)],boardQ=leg.points[Math.min(leg.points.length-1,i)],horizontal=Math.abs((boardQ?.x||0)-(boardPrev?.x||0))>=Math.abs((boardQ?.y||0)-(boardPrev?.y||0));orth.push(horizontal?{x:q.x,y:prev.y}:{x:prev.x,y:q.y})}orth.push(q)}return orth}
function cbAsFitSegments(){const out=[];for(const c of cbCircuits().filter(c=>c.complete)){for(const leg of c.legs||[]){const pts=cbPlanPathForLeg(c,leg);for(let i=1;i<pts.length;i++)out.push({a:pts[i-1],b:pts[i],color:c.color||'#2675db'})}}return out}
function cbDrawAsFitOn(canvas,W,H,header=false){const x=canvas.getContext('2d');x.setTransform(1,0,0,1,0,0);x.clearRect(0,0,W,H);x.fillStyle='#fff';x.fillRect(0,0,W,H);let top=header?78:16,bottom=header?76:16;if(header){x.fillStyle='#16283e';x.fillRect(0,0,W,64);x.fillStyle='#fff';x.font='800 24px system-ui';x.fillText((state.site||'Site')+' — '+activeFloor().name+' — AS-FIT',30,29);x.font='13px system-ui';x.fillText('Generated from Zone Plan + Site Survey + completed Circuit Builder routes',30,50)}const aw=W-50,ah=H-top-bottom,aspect=img?img.width/img.height:1.6;let pw=aw,ph=pw/aspect;if(ph>ah){ph=ah;pw=ph*aspect}const ox=(W-pw)/2,oy=top+(ah-ph)/2,map=p=>({x:ox+p.x*pw,y:oy+p.y*ph});x.strokeStyle='#344b63';x.lineWidth=Math.max(1.2,W/1800*2);for(const wall of state.walls||[]){if(wall.kind!=='wall'||wall.points?.length<2)continue;stroke(x,wall.points.map(map),x.lineWidth)}for(const door of state.doors||[]){const a=map(door.a),b=map(door.b);door.double?drawDoubleDoor(x,a,b,x.lineWidth,door.side||1):drawDoor(x,a,b,x.lineWidth,door.side||1)}for(const win of state.windows||[])drawWindow(x,map(win.a),map(win.b),x.lineWidth);for(const sh of state.shutters||[])drawShutter(x,map(sh.a),map(sh.b),x.lineWidth);for(const st of state.stairs||[])drawStairs(x,map(st.a),map(st.b),x.lineWidth);x.fillStyle='#506277';x.textAlign='center';for(const l of state.labels||[]){const p=map(l);roomLabel(x,p.x,p.y,l.text,Math.max(10,W/170)*(l.scale||1))}const segments=cbAsFitSegments();cbDrawBundled(x,segments,map);const size=Math.min(pw,ph)*.018;for(const s of state.symbols||[]){if(!(symbolScope(s)==='survey'||s.type==='panel'||s.type==='repeater'))continue;const p=map(s);if(s.type==='beam'&&s.beamEnd)drawBeamTriangle(x,p,map(s.beamEnd),size*(s.scale||1),s.color||'#172333');else drawSymbol(x,p,s.type,size*(s.scale||1),s.color||'#172333',s.rotation||0);if(s.reference)symbolReference(x,p,s.reference,size*(s.scale||1))}if(header){let lx=30,ly=H-35;x.textAlign='left';x.font='700 13px system-ui';for(const c of cbCircuits().filter(c=>c.complete)){x.fillStyle=c.color||'#2675db';x.fillRect(lx,ly-11,18,5);x.fillStyle='#25394f';x.fillText(c.name+' · '+(c.type==='addressable'?'loop':'conventional'),lx+25,ly);lx+=Math.min(320,x.measureText(c.name).width+150);if(lx>W-280){lx=30;ly-=22}}}}
function cbDrawAsFitPreview(){const c=$('cbAsFitCanvas'),r=c.getBoundingClientRect(),dpr=Math.min(devicePixelRatio||1,2),W=Math.max(1,Math.round(r.width*dpr)),H=Math.max(1,Math.round(r.height*dpr));if(c.width!==W)c.width=W;if(c.height!==H)c.height=H;const x=c.getContext('2d');x.setTransform(dpr,0,0,dpr,0,0);x.clearRect(0,0,r.width,r.height);const tmp=document.createElement('canvas');tmp.width=Math.max(1,Math.round(r.width));tmp.height=Math.max(1,Math.round(r.height));cbDrawAsFitOn(tmp,tmp.width,tmp.height,false);x.drawImage(tmp,0,0,r.width,r.height)}
function cbFinishAsFit(){const list=cbCircuits();if(!list.length||!list.every(c=>c.complete))return;const used=cbUsedIds(),unassigned=cbSurveyDevices().filter(d=>!used.has(d.id));if(unassigned.length&&!confirm(unassigned.length+' surveyed device'+(unassigned.length===1?' is':'s are')+' not assigned to a completed circuit. Build the As-Fit anyway?'))return;state.asFit={generatedAt:Date.now(),circuitIds:list.filter(c=>c.complete).map(c=>c.id)};persist();cbShow('asfit')}
async function cbExportAsFit(){const base=Math.max(1800,Math.min(3600,img?.width||2400)),aspect=img?img.width/img.height:1.6,W=Math.round(base),H=Math.round(W/aspect+170),c=document.createElement('canvas');c.width=W;c.height=H;cbDrawAsFitOn(c,W,H,true);const safe=(state.site||'site').replace(/[^a-z0-9 -]/gi,'').trim().replace(/\s+/g,'-').slice(0,45)||'site',floor=activeFloor().name.replace(/[^a-z0-9 -]/gi,'').trim().replace(/\s+/g,'-');await shareCanvas(c,safe+'-'+floor+'-As-Fit.png',(state.site||'Site')+' '+activeFloor().name+' As-Fit','As-Fit wiring drawing generated from the completed Circuit Builder routes.')}
$('circuitModeBtn').onclick=openCircuitBuilder;$('cbBack').onclick=closeCircuitBuilder;$('cbGameHome').onclick=()=>{cbCircuit=null;cbDrag=null;cbShow('home')};$('cbAddressableStart').onclick=cbStartAddressable;$('cbBuildSelected').onclick=cbBuildAddressable;$('cbUndoLeg').onclick=cbUndo;$('cbReset').onclick=cbResetCurrent;$('cbComplete').onclick=cbCompleteCircuit;$('cbFinishAsFit').onclick=cbFinishAsFit;$('cbAsFitBack').onclick=()=>cbShow('home');$('cbExportAsFit').onclick=cbExportAsFit;$('cbCanvas').onpointerdown=cbCanvasDown;$('cbCanvas').onpointermove=cbCanvasMove;$('cbCanvas').onpointerup=cbCanvasUp;$('cbCanvas').onpointercancel=()=>{cbDrag=null;cbHover=null;cbDrawBoard()};window.addEventListener('resize',()=>{if(!$('circuitBuilder').hidden){if(cbScreen==='asfit')cbDrawAsFitPreview();else if(cbScreen==='game'||cbScreen==='select')cbDrawBoard()}});

'''
    text = replace_once(text, 'function syncSurveyTools(){', js + 'function syncSurveyTools(){', 'Circuit Builder logic')

p.write_text(text)
for target in [Path('ZoneSketch.html'), Path('Zone-Sketch-by-Will.html'), Path('app/src/main/assets/index.html')]:
    target.write_text(text)

gradle = Path('app/build.gradle')
g = gradle.read_text()
g = g.replace('versionCode 43', 'versionCode 44').replace("versionName '0.42'", "versionName '0.43'")
gradle.write_text(g)
print('Prepared v0.43 Circuit Builder foundation')
